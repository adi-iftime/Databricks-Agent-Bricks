name: Databricks CI/CD

on:
  pull_request:
    branches: [main, master, dev]
  push:
    branches: [main, master, dev]
  workflow_dispatch:

concurrency:
  group: databricks-cicd-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
  DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dev dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

  lint:
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dev dependencies
        run: pip install -e ".[dev]"

      - name: Ruff check (hook modules)
        run: ruff check .cursor/hooks

  unit-test:
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run unit tests
        run: python3 -m unittest discover -s tests -p "test_*.py" -v

  databricks-auth:
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Databricks CLI
        uses: databricks/setup-cli@v0.267.0

      - name: Verify Databricks authentication
        run: databricks current-user me

  bundle-validate:
    needs: [setup, databricks-auth]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Bundle manifest structure check
        run: python3 -m unittest tests.test_databricks_bundle tests.test_job_resources -v

      - name: Set up Databricks CLI
        uses: databricks/setup-cli@v0.267.0

      - name: Databricks bundle validate (dev target)
        run: databricks bundle validate -t dev

  deploy:
    needs: [lint, unit-test, databricks-auth, bundle-validate]
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy uses databricks-deploy-dev.yml on push to dev (SCRUM-123)."
