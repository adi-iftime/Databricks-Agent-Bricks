resources:
  jobs:
    agent_analysis:
      name: "[${bundle.target}] mlops agent analysis"
      tasks:
        - task_key: stub
          notebook_task:
            notebook_path: ../../notebooks/agent/stub.py
