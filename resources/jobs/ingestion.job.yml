resources:
  jobs:
    ingestion:
      name: "[${bundle.target}] mlops ingestion"
      tasks:
        - task_key: stub
          notebook_task:
            notebook_path: ../../notebooks/ingestion/stub.py
