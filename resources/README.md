# Bundle resources

Declarative bundle assets deployed via DAB:

- `jobs/` — Lakeflow job definitions
- `sql/` — Unity Catalog DDL and grants
- `grants/` — RBAC templates (Epic 4 / 10)

Resource files use `<name>.<resource_type>.yml` naming per Databricks bundle conventions.
