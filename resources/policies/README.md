# Cluster policies (DAB)

Policy definitions are managed in the Databricks workspace. This directory documents policy IDs referenced by bundle variables — do not commit policy JSON with secrets.

Set `cluster_policy_id` per target in `databricks.yml` after your platform team provisions policies.

See [docs/operations/cluster-policies.md](../../docs/operations/cluster-policies.md).
