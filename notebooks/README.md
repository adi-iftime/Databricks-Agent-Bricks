# Notebooks

Deployable Databricks notebooks referenced by the Asset Bundle (`databricks.yml`).

Organize by domain: `ingestion/`, `anomaly/`, `agent/`, etc. Notebook paths are wired in bundle job resources (see SCRUM-128).
